# Automation-Projects
This script is for a remote server that uses VPN connection to grab SQL backup files from three different main office server locations and different directories.
Pre-requisites:
  VPN setup
  Credentials setup first in Windows Credentials Manager
  This is powershell script that needs to run in Windows Environment.
